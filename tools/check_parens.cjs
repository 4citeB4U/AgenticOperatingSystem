const fs = require('fs'); const s = fs.readFileSync('src/App.tsx','utf8'); let paren=0; for(let i=0;i<s.length;i++){ const c=s[i]; if(c==='(') paren++; else if(c===')') paren--; if(paren<0){ console.log('Negative paren at pos',i); console.log('context', s.slice(Math.max(0,i-40), i+40)); break; } } console.log('final paren', paren); let brace=0; for(let i=0;i<s.length;i++){ const c=s[i]; if(c==='{') brace++; else if(c==='}') brace--; if(brace<0){ console.log('Negative brace at pos',i); console.log('context', s.slice(Math.max(0,i-40), i+40)); break; } } console.log('final brace', brace);